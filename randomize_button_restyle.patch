--- a/Assets/Scripts/UI_Import/GroomVarianceController.cs	2026-08-18 09:05:02.554903858 +0000
+++ b/Assets/Scripts/UI_Import/GroomVarianceController.cs	2026-08-18 09:05:59.035226646 +0000
@@ -259,7 +259,7 @@
         TextMeshProUGUI seedLabel = AddText(seedRowGO.transform, "SEED", 10, 38);
         seedLabel.alignment = TextAlignmentOptions.Center;
         TMP_InputField seedInput = AddSeedField(seedRowGO.transform, 78);
-        GameObject randomButton = AddButton(seedRowGO.transform, "R", 24);
+        GameObject randomButton = AddButton(seedRowGO.transform, "RANDOMIZE", 92, 19);
 
         // Thin divider between this channel's block and the next, so each setting's variance
         // controls read as one clearly separated group rather than running into each other.
@@ -574,22 +574,22 @@
         return input;
     }
 
-    GameObject AddButton(Transform p, string label, float width)
+    GameObject AddButton(Transform p, string label, float width, float height = 24f)
     {
         GameObject go = new GameObject(label + "Button", typeof(RectTransform), typeof(Image), typeof(Button));
         go.transform.SetParent(p, false);
-        go.GetComponent<RectTransform>().sizeDelta = new Vector2(width, 24);
+        go.GetComponent<RectTransform>().sizeDelta = new Vector2(width, height);
 
-        // Brighter than the standard muted button tint and sized closer to the seed field's own
-        // 24px height, rather than leaving it at the default width/tint every other button gets
-        // from the global reskin pass - this one's a reroll action and should read as distinct.
+        // Deliberately self-styled with a bright teal tint (UIThemeAuthority skips this button
+        // by name so the shared white-tint pass doesn't overwrite it). The previous cyan leaned
+        // blue against the teal sprite; this tint stays in the teal family, just brighter.
         Image image = go.GetComponent<Image>();
         Button button = go.GetComponent<Button>();
         if (UITheme.ButtonNormalSprite != null)
         {
             image.sprite = UITheme.ButtonNormalSprite;
             image.type = Image.Type.Sliced;
-            image.color = new Color(.45f, .95f, 1f, 1f);
+            image.color = new Color(.62f, 1f, .96f, 1f);
             button.transition = Selectable.Transition.SpriteSwap;
             SpriteState state = button.spriteState;
             state.highlightedSprite = UITheme.ButtonHoverSprite;
@@ -601,7 +601,7 @@
             image.color = new Color(.27f, .34f, .20f);
         }
 
-        TextMeshProUGUI text = AddText(go.transform, label, 12, width); RectTransform tr = text.rectTransform; tr.anchorMin = Vector2.zero; tr.anchorMax = Vector2.one; tr.offsetMin = Vector2.zero; tr.offsetMax = Vector2.zero;
+        TextMeshProUGUI text = AddText(go.transform, label, 11, width); RectTransform tr = text.rectTransform; tr.anchorMin = Vector2.zero; tr.anchorMax = Vector2.one; tr.offsetMin = Vector2.zero; tr.offsetMax = Vector2.zero;
         return go;
     }
 }
--- a/Assets/Scripts/UI_Import/UIThemeAuthority.cs	2026-08-18 09:05:02.560107761 +0000
+++ b/Assets/Scripts/UI_Import/UIThemeAuthority.cs	2026-08-18 09:05:29.905079482 +0000
@@ -44,6 +44,12 @@
         foreach (Button button in FindObjectsByType<Button>(FindObjectsInactive.Include, FindObjectsSortMode.None))
         {
             if (button == null) continue;
+
+            // Buttons that deliberately style themselves (currently the variance RANDOMIZE
+            // reroll) opt out of the shared skin here, otherwise this pass overwrites their
+            // custom tint with the standard white one the moment they're created.
+            if (button.gameObject.name == "RANDOMIZEButton") continue;
+
             if (!styledButtons.Contains(button))
             {
                 UITheme.StyleButton(button);
