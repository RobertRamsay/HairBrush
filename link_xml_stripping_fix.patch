diff --git a/Assets/link.xml b/Assets/link.xml
new file mode 100644
index 0000000..4932969
--- /dev/null
+++ b/Assets/link.xml
@@ -0,0 +1,28 @@
+<linker>
+  <assembly fullname="Assembly-CSharp">
+    <!-- Reflected into by RuntimeBuildLoadAuthority (allGroupIds, groupNames, groupUScales/
+         VScales/UOffsets/VOffsets, loadedModel, currentModelPath, activeSliderPanel,
+         isGroomingMode, BuildGroupManagementUI) and by several other authority scripts. -->
+    <type fullname="ModelViewer" preserve="all" />
+
+    <!-- Reflected into by GroupClumperPersistenceBridge and others for clumper restore. -->
+    <type fullname="GroupClumperManager" preserve="all" />
+
+    <!-- Reflected into by GroupUVRangeSliderUI (SetRangeValue) and related UV row scripts. -->
+    <type fullname="GroupPredeterminedUVController" preserve="all" />
+
+    <!-- Reflected into for hair card state/mesh access by several authorities. -->
+    <type fullname="HairCard" preserve="all" />
+
+    <type fullname="MaterialEditorManager" preserve="all" />
+    <type fullname="PlacementBrushModeAuthority" preserve="all" />
+    <type fullname="PostAffectorManager" preserve="all" />
+    <type fullname="PostPredeterminedUVAuthority" preserve="all" />
+    <type fullname="PostVarianceAffectorBridge" preserve="all" />
+    <type fullname="GroomRootStateAuthority" preserve="all" />
+    <type fullname="GroomShapeCurveAuthority" preserve="all" />
+    <type fullname="GroomVarianceController" preserve="all" />
+    <type fullname="GroomCurveGraphInput" preserve="all" />
+    <type fullname="GroomCurvePointHandle" preserve="all" />
+  </assembly>
+</linker>
